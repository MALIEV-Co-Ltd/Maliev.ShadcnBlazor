namespace Maliev.ShadcnBlazor.Components.Selection;

/// <summary>Controls the visual surface used by radio-group options.</summary>
public enum ShadcnRadioGroupPresentation
{
    /// <summary>Uses the compact radio and label presentation.</summary>
    Default,
    /// <summary>Uses a full option-card surface that communicates selection.</summary>
    Card
}
