namespace Maliev.ShadcnBlazor.Components.Styling;

/// <summary>Defines the bounded strength of the selected visual treatments.</summary>
public enum ShadcnStyleIntensity
{
    /// <summary>Uses restrained treatment values.</summary>
    Subtle,

    /// <summary>Uses the standard treatment values.</summary>
    Default,

    /// <summary>Uses the strongest supported treatment values.</summary>
    Strong
}
