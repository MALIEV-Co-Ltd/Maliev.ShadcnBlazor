@inherits Maliev.ShadcnBlazor.Components.Primitives.ShadcnComponentBase
<div @ref="_root" @attributes="ForwardedAttributes" class="@MergeClass("shadcn-message-scroller")" style="@MergeStyle(null)" data-slot="message-scroller" data-scrollable-start="@(Owner?.State.Scrollable.Start==true?"true":"false")" data-scrollable-end="@(Owner?.State.Scrollable.End==true?"true":"false")" data-following="@(Owner?.State.Following==true?"true":"false")" data-unread="@(Owner?.State.Unread==true?"true":null)"><CascadingValue Value="this" IsFixed="true">@ChildContent</CascadingValue></div>
@code { [CascadingParameter] private ShadcnMessageScrollerProvider? Owner{get;set;} [Parameter,EditorRequired] public RenderFragment? ChildContent{get;set;} private ElementReference _root; private IReadOnlyDictionary<string,object> ForwardedAttributes=>AttributesExcept("data-slot","data-scrollable-start","data-scrollable-end","data-following","data-unread"); protected override void OnParametersSet(){if(Owner is null||ChildContent is null)throw new InvalidOperationException("ShadcnMessageScroller requires a provider and content.");} protected override async Task OnAfterRenderAsync(bool firstRender){if(firstRender)await Owner!.AttachAsync(_root);} }
