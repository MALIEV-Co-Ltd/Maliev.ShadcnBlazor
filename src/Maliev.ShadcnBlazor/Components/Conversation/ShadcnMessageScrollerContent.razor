@inherits Maliev.ShadcnBlazor.Components.Primitives.ShadcnComponentBase
<div @attributes="ForwardedAttributes" class="@MergeClass("shadcn-message-scroller-content")" style="@MergeStyle(null)" data-slot="message-scroller-content" aria-live="@AriaLive"><CascadingValue Value="this" IsFixed="true">@ChildContent</CascadingValue></div>
@code { [CascadingParameter] private ShadcnMessageScrollerViewport? Owner{get;set;} [Parameter] public string AriaLive{get;set;}="off"; [Parameter,EditorRequired] public RenderFragment? ChildContent{get;set;} private IReadOnlyDictionary<string,object> ForwardedAttributes=>AttributesExcept("data-slot","aria-live"); protected override void OnParametersSet(){if(Owner is null||ChildContent is null)throw new InvalidOperationException("ShadcnMessageScrollerContent requires a viewport and content.");if(AriaLive is not("off" or "polite" or "assertive"))throw new ArgumentOutOfRangeException(nameof(AriaLive));} }
