@inherits Maliev.ShadcnBlazor.Components.Primitives.ShadcnComponentBase
<CascadingValue Value="this" IsFixed="true"><span @attributes="Forwarded" class="@MergeClass("shadcn-context-menu-sub")" style="@MergeStyle(null)" data-slot="context-menu-sub" data-state="@(Open?"open":"closed")">@ChildContent</span></CascadingValue>
@code { [Parameter]public bool Open{get;set;}[Parameter]public EventCallback<bool> OpenChanged{get;set;}[Parameter]public RenderFragment? ChildContent{get;set;}private static long _next;private readonly long _id=Interlocked.Increment(ref _next);internal string TriggerId=>$"shadcn-context-sub-{_id}-trigger";internal string ContentId=>$"shadcn-context-sub-{_id}-content";private IReadOnlyDictionary<string,object> Forwarded=>AttributesExcept("data-slot","data-state");internal async ValueTask SetOpenAsync(bool value){if(OpenChanged.HasDelegate)await OpenChanged.InvokeAsync(value);else{Open=value;await InvokeAsync(StateHasChanged);}} }
