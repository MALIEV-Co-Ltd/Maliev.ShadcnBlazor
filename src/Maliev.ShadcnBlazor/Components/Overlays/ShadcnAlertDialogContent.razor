@inherits Maliev.ShadcnBlazor.Components.Primitives.ShadcnComponentBase
@implements IAsyncDisposable
@inject IJSRuntime JavaScript
@if (Context.Owner.EffectiveOpen)
{
    <div class="shadcn-alert-dialog-portal" data-slot="alert-dialog-portal"><div class="shadcn-alert-dialog-overlay" data-slot="alert-dialog-overlay" data-state="open"></div><div @ref="_element" @attributes="ForwardedAttributes" id="@Context.ContentId" class="@MergeClass("shadcn-alert-dialog-content")" style="@MergeStyle(null)" data-slot="alert-dialog-content" data-state="open" data-size="@SizeValue" role="alertdialog" aria-modal="true" aria-labelledby="@Context.TitleId" aria-describedby="@Context.DescriptionId" tabindex="-1">@ChildContent</div></div>
}
@code {
    [CascadingParameter] private ShadcnAlertDialogContext Context { get; set; } = default!; [Parameter] public ShadcnAlertDialogSize Size { get; set; } [Parameter] public RenderFragment? ChildContent { get; set; }
    private ElementReference _element; private IJSObjectReference? _module; private DotNetObjectReference<ShadcnAlertDialogContent>? _selfReference; private bool _attached; private bool _ariaSynchronized; private string? _attachedElementId;
    private string SizeValue => Size switch { ShadcnAlertDialogSize.Default => "default", ShadcnAlertDialogSize.Small => "sm", _ => throw new ArgumentOutOfRangeException(nameof(Size), Size, "Unknown alert dialog size.") };
    private IReadOnlyDictionary<string, object> ForwardedAttributes => AttributesExcept("id", "data-slot", "data-state", "data-size", "role", "aria-modal", "aria-labelledby", "aria-describedby", "tabindex");
    protected override void OnParametersSet() => _ = SizeValue;
    protected override async Task OnAfterRenderAsync(bool firstRender) { if (!Context.Owner.EffectiveOpen) { await DetachAsync(); return; } if (Context.TitleId is null) throw new InvalidOperationException("ShadcnAlertDialogContent requires a ShadcnAlertDialogTitle."); if (!_ariaSynchronized) { _ariaSynchronized = true; await InvokeAsync(StateHasChanged); return; } if (_attached && _attachedElementId == _element.Id && _module is not null && await _module.InvokeAsync<bool>("isDialogAttached", _element)) return; if (_attached) await ResetInteropAsync(); _module = await JavaScript.InvokeAsync<IJSObjectReference>("import", "./_content/Maliev.ShadcnBlazor/js/shadcn-overlays-menus.js"); _selfReference=DotNetObjectReference.Create(this); await _module.InvokeVoidAsync("attachDialog", _element, _selfReference, true, Context.Owner.CloseOnEscape); _attached = true; _attachedElementId = _element.Id; }
    [JSInvokable] public Task RequestCloseAsync() => Context.Owner.SetOpenAsync(false).AsTask();
    private async ValueTask DetachAsync() { if (_module is not null) { try { if (_attached) await _module.InvokeVoidAsync("detachDialog", _element); await _module.DisposeAsync(); } catch (JSDisconnectedException) { } } ResetInterop(); }
    private async ValueTask ResetInteropAsync() { if (_module is not null) { try { await _module.DisposeAsync(); } catch (JSDisconnectedException) { } } ResetInterop(); }
    private void ResetInterop() { _selfReference?.Dispose(); _selfReference=null; _module=null; _attached=false; _attachedElementId=null; }
    public ValueTask DisposeAsync() => DetachAsync();
}
