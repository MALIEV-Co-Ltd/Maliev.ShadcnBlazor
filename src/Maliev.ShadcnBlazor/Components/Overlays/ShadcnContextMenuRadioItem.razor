@inherits Maliev.ShadcnBlazor.Components.Primitives.ShadcnFocusableComponentBase
<div @ref="FocusElement" @attributes="Forwarded" class="@MergeClass("shadcn-context-menu-radio-item")" style="@MergeStyle(null)" data-slot="context-menu-radio-item" data-disabled="@(Disabled?"true":null)" role="menuitemradio" aria-checked="@(Selected?"true":"false")" aria-disabled="@(Disabled?"true":null)" tabindex="-1" @onclick="ClickAsync"><span data-slot="context-menu-radio-item-indicator" aria-hidden="true">@if(Selected){<svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="3"></circle></svg>}</span>@ChildContent</div>
@code { [CascadingParameter]private ShadcnMenuRadioContext? Context{get;set;}[Parameter,EditorRequired]public string Value{get;set;}=string.Empty;[Parameter]public bool Disabled{get;set;}[Parameter]public RenderFragment? ChildContent{get;set;}private bool Selected=>Context?.Value==Value;private IReadOnlyDictionary<string,object> Forwarded=>AttributesExcept("data-slot","data-disabled","role","aria-checked","aria-disabled","tabindex");protected override void OnParametersSet()=>_=ShadcnMenuValues.RequireValue(Value);private async Task ClickAsync(){if(!Disabled&&Context is not null)await Context.SelectAsync(Value);} }
